<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TCar</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <header>
        <div class="header-content">
            <h1 class="logo">TCar</h1>
            <nav>
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/about">About</a></li>
                    <li><a href="/contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <div class="search-section">
            <form class="search-bar">
                <input type="text" placeholder="Search for cars...">
                <button type="submit"><i class="fa fa-search"></i> Search</button>
            </form>
        </div>
        
        <section class="car-list">
            <% cars.forEach(car => { %>
                <div class="car-item">
                    <img src="<%= car.image %>" alt="<%= car.name %>" class="car-image">
                    <h2><%= car.name %></h2>
                    <p><%= car.description %></p>
                    <p><strong>Price:</strong> $<%= car.price %></p>
                    <p><strong>Make:</strong> <%= car.make %> | <strong>Year:</strong> <%= car.model_year %></p>
                    <form action="/car/<%= car.id %>" method="GET">
                        <button type="submit" class="btn-detail">View Details</button>
                    </form>
                </div>
            <% }); %>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 DailyCar. All rights reserved.</p>
    </footer>
</body>
</html>
